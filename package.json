{
  "name": "no-bs-ftp-mcp",
  "private": true,
  "type": "module",
  "license": "MIT",
  "version": "1.0.0",
  "description": "API for MCP Streamable HTTP transport with JSON-RPC 2.0 for S3-compatible object storage operations. Supports bucket and key based identity, S3-style pagination, structured tags, and multipart uploads. Designed for production use with proper error handling and OpenAI-compatible LLM orchestration support.",
  "scripts": {
    "dev": "node --loader ts-node/esm src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "express": "^5.2.1",
    "node-fetch": "^3.3.2",
    "zod": "^4.3.6"
  }
}
